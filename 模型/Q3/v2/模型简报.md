# Q3 模型简报

owner_name: 罗懿
owner_code: L
owner_role: 建模手
version: v2
status: draft
based_on: 模型/Q3/v2/建模思路.md；模型/Q3/v2/数学模型.md；模型/Q3/v2/约束.md；模型/Q3/v2/结果需求.md

## 摘要

Q3 的正式模型是四自由度非线性系统。状态为 `(z_f,θ_f,ξ,θ_o)` 及其一阶导数。加速度由式（Q3-15）从质量矩阵线性方程组解出，右端使用已经展开的 `h、∇V、∂R/∂q̇`，不要再对动能或势能做符号求导。线性式（Q3-17）只作校验。输出按 `result3.xlsx` 写成：时间，浮子四项，振子四项；振子垂荡必须用式（Q3-22）、式（Q3-23）还原。

## 待确认项

无。下列事项已锁定，编程阶段不得改口：

- 正式结果积分式（Q3-16），不用线性解提交。
- `I_f=8398.7761`，`d=-1.4079252`，其余锁定常数见数学模型符号表。
- 附件 3 的纵摇系数加在浮子质心横轴上，交叉水动力为 0。
- `ξ` 越界或 `M` 不正定则结果无效，不改方程。

积分器名称、内部步长和局部容差不属于建模待确认项，由算法阶段确定，但不得把 `0.2 s` 当作积分步长。

## 交接说明

数学源是 `模型/Q3/v2/数学模型.md` 的式（Q3-1）至式（Q3-24）。编程手应：

1. 把锁定常数写成常量，不要重算质心或惯量。
2. 实现 `M(q)`、式（Q3-12）至式（Q3-14），再解式（Q3-15）。
3. 从 `y(0)=0` 积分到 `t_max=40(2π/ω)`，仅在 `t_n=0.2n` 取值。
4. 用式（Q3-22）、式（Q3-23）得到振子垂荡，按结果需求的 A–I 列写入模板。
5. 另算线性对照和能量恒等式，写入检查记录，不覆盖 `result3.xlsx`。

遇到公式未覆盖的力学选择，停下来问建模手，不要自行补项。

## 论文数学模型入口

尚未执行 `paper-model-distillation`。当前详细模型是编程交接源，不是论文正文。
