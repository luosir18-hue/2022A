# Q2 约束

owner_name: 罗懿
owner_code: L
owner_role: 建模手
version: v1
status: draft
based_on: 模型/Q2/v1/数学模型.md

## 单位约定

沿用 Q1；功率单位为 `W`。旋转量不参与本问。幂律比例系数 `c₀` 的量纲必须使 `c₀|v_r|^p v_r` 为力。

## 目标

分别最大化常量阻尼和幂律阻尼下 PTO 的稳态平均机械输出功率。

## 硬约束

- `ω=2.2143 s⁻¹`，使用附件 3 的问题 2 参数行。
- `0≤c≤100000`；`0≤c₀≤100000`；`0≤p≤1`。
- 功率必须由阻尼力乘相对速度得到，且取稳态时间平均。
- 被评价轨迹必须满足 Q2 动力学方程并保持有界。

## 软约束

- 相邻稳态周期的状态与平均功率应收敛；具体阈值留给 `algorithm-spec` 明确。
- 最优点应通过边界比较、局部加密和至少一种独立计算方式复核。

## 边界与可行域

可行域为 `[0,100000]` 或 `[0,100000]×[0,1]`。平均窗口、收敛阈值和异常参数处理在用户确认稳态口径后进入算法合同，当前不得用任意固定时长替代。
